#include <stdio.h>
#include <string.h>

int main() {
    char A[11], B[11];
    
    scanf("%s", A);
    scanf("%s", B);

    int sizeA = strlen(A);
    int sizeB = strlen(B);

    char concatenated[21];
    strcpy(concatenated, A);
    strcat(concatenated, B);


    char temp = A[0];
    A[0] = B[0];
    B[0] = temp;


    printf("%d
