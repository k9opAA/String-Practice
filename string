#include <stdio.h>
#include <string.h>

int main() {
    char A[11], B[11];
    
    // Read the input strings
    scanf("%s", A);
    scanf("%s", B);

    // Calculate the sizes of strings A and B
    int sizeA = strlen(A);
    int sizeB = strlen(B);

    // Concatenate strings A and B
    char concatenated[21];
    strcpy(concatenated, A);
    strcat(concatenated, B);

    // Swap the first characters of A and B
    char temp = A[0];
    A[0] = B[0];
    B[0] = temp;

    // Print the results
    printf("%d
